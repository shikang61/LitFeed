-- Category preference weights live in kv.key = 'category_preferences' (JSON object).
-- No new tables; this file documents the convention for operators.

-- Example inspect:
--   SELECT value FROM kv WHERE key = 'category_preferences';
